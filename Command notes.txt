{
<p> used for making paragraphs.

attributes:
	id: used to link to the certain parts of the page. It can go to top and bottom.
}
<span> contains short pieces of text or other HTML. They are used to separate small pieces of content that are on the same line as other content.
The <em> tag will generally render as italic emphasis.
The <strong> will generally render as bold emphasis.
<h#> will set text size according to the bases of 1 being biggest to 6 being being smallest. (# is no.)
<br> is next line.
	{
<ul> to create a list of items in no particular order.

Individual list items must be added to the unordered list using the <li> tag. The <li> or list item tag is used to describe an item in a list.

example: 
<ul>
  <li>Limes</li>
  <li>Tortillas</li>
  <li>Chicken</li>
</ul>

 (<ol>) are like unordered lists, except that each list item is numbered.
example:
<ol>
  <li>Preheat the oven to 350 degrees.</li>
  <li>Mix whole wheat flour, baking soda, and salt.</li>
  <li>Cream the butter, sugar in separate bowl.</li>
  <li>Add eggs and vanilla extract to bowl.</li>
</ol>

	}
{
<img src="image-location.jpg" alt="image description"> for adding images. You have to specify directory. It can be url or local directory based. closing tag not req as it is self closing tag.

attribute:
	src: source
	alt: description of the text, or the text found on hovering on the picture.

<video src="myVideo.mp4" width="320" height="240" controls>
  Video not supported
</video>
	
atrribute:
	src: source
	width: width of the video frame
	height: height of the video frame
	controls: enables controls.
Note: The message in between is shown incase the video is not able to be loaded. It can be url or local directory based.
}

<!DOCTYPE html> to let web browsers know that html is being used.

{
The <head> element contains the metadata for a web page. Metadata is information about the page that isn’t displayed directly on the web page. 

Function tags:
	<title>: It carries the title for the webpage shown on a tab. 
}

{
<a href="https://www.wikipedia.org/">This Is A Link To Wikipedia</a>: The <a> tag in HTML is used to create hyperlinks, allowing users to navigate to another webpage or resource by clicking on it. 

atrributes:
	href: used to specify the destination URL.
	target: decides how to open the tab.
	alt: description of the text, or the text found on hovering on the media

More info:
	target:
		_blank: open the link in a new tab
		For example: <a href="https://en.wikipedia.org/wiki/Brown_bear" target="_blank">The Brown Bear</a>

	href: Can be used to link other html files. Mostly used to make the files more neat.
		For example: <a href="./contact.html">Contact</a>

note: You can make anything hyperlink until it is inside the tag.
}
{
<div>: used to divide certain parts of the webite or words.
attribute:
	id: assigns an value that can be used with parts of code. the useable parts are give in Special Note Section.
}

Comments: Comments begin with <!-- and end with -->. Any characters in between will be ignored by your browser.

{
<table> used to make tables
attribute:
	border: used to add a border and feed the thickness of the border.

sub-element tags:
	<tr>: used to make rows. requires closing tag.

sub-element of sub-element tags:
	<td>: used to enter data
	<th>: used to give title to table, can be given to row(row) or column(col)
		attribute:
			scope: used to place the title to row or column. value row for row and col for column
}
Special Note section:{
[the id attribute's value in the <div> tag can be used with href attribute of the <a> tag. the id value in href has to been mentioned by typing #(id)]
}